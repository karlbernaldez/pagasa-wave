import { Stage, Layer, Line, RegularPolygon, Wedge } from 'react-konva';
import { useRef, useState, useEffect, useCallback } from 'react';
import { v4 as uuidv4 } from 'uuid';
import Swal from 'sweetalert2';
import { CheckCircle2, CloudSun, GripHorizontal, RotateCcw, Snowflake, Waves } from 'lucide-react';
import { createFeature } from '@/api/featureServices';
import { useProjectId } from '@dashboards/forecaster/hooks/useStudio';

const COLORS = { cold: '#1d4ed8', warm: '#ef4444', occluded: '#7c3aed' };
const FRONT_TYPES = {
  cold: { label: 'Cold', fullLabel: 'Cold Front', color: COLORS.cold, lineWidth: 4, spacing: 46, icon: Snowflake, symbols: [{ kind: 'triangle', color: COLORS.cold, side: -1 }] },
  warm: { label: 'Warm', fullLabel: 'Warm Front', color: COLORS.warm, lineWidth: 4, spacing: 46, icon: CloudSun, symbols: [{ kind: 'semicircle', color: COLORS.warm, side: -1 }] },
  stationary: { label: 'Stationary', fullLabel: 'Stationary Front', color: COLORS.cold, lineWidth: 3, spacing: 42, icon: Waves, symbols: [{ kind: 'semicircle', color: COLORS.warm, side: -1 }, { kind: 'triangle', color: COLORS.cold, side: 1 }] },
  occluded: { label: 'Occluded', fullLabel: 'Occluded Front', color: COLORS.occluded, lineWidth: 4, spacing: 42, icon: CheckCircle2, symbols: [{ kind: 'semicircle', color: COLORS.occluded, side: -1 }, { kind: 'triangle', color: COLORS.occluded, side: -1 }] },
};

const PANEL_WIDTH = 430;
const PANEL_HEIGHT = 146;
const STORAGE_KEY = 'wavelab-surface-front-panel-position-v1';
const MIN_POINT_DISTANCE = 3.5;
const FRONT_SYMBOL_RADIUS = 9;
const FRONT_TRIANGLE_SIZE = 10;
const STATIONARY_SEGMENT_LENGTH = 28;
const STATIONARY_SEGMENT_GAP = 3;
const STATIONARY_SEGMENT_COLORS = [COLORS.warm, COLORS.cold];

const cn = (...classes) => classes.filter(Boolean).join(' ');
const normalizeFrontType = (frontType) => (FRONT_TYPES[frontType] ? frontType : 'cold');
const getViewportWidth = () => (typeof window === 'undefined' ? 1440 : window.innerWidth);
const getViewportHeight = () => (typeof window === 'undefined' ? 900 : window.innerHeight);
const getDefaultPosition = () => ({ x: 300, y: 100 });
const getSafePosition = (position) => ({ x: Math.min(Math.max(position.x, 16), Math.max(16, getViewportWidth() - PANEL_WIDTH - 16)), y: Math.min(Math.max(position.y, 72), Math.max(72, getViewportHeight() - PANEL_HEIGHT - 16)) });
const getMapContainerRect = (mapRef) => mapRef?.current?.getContainer?.()?.getBoundingClientRect?.() || { left: 0, top: 0, width: getViewportWidth(), height: getViewportHeight() };
const getStageBounds = (mapRef) => { const rect = getMapContainerRect(mapRef); return { left: rect.left || 0, top: rect.top || 0, width: rect.width || getViewportWidth(), height: rect.height || getViewportHeight() }; };
const getPointerPosition = (event) => event.target.getStage().getPointerPosition();
const buildFrontName = (frontType) => FRONT_TYPES[normalizeFrontType(frontType)]?.fullLabel || 'Surface Front';
const getDistanceFromLastPoint = (points, x, y) => { if (!Array.isArray(points) || points.length < 2) return Infinity; return Math.hypot(x - points[points.length - 2], y - points[points.length - 1]); };

function addFrontLineLayer(map, layerId, sourceId, paint) { if (map.getLayer(layerId)) return; map.addLayer({ id: layerId, type: 'line', source: sourceId, layout: { 'line-join': 'round', 'line-cap': 'round', visibility: 'visible' }, paint }); }
function removeFrontLayers(map, sourceId) { [`${sourceId}_dash`, `${sourceId}_secondary`, `${sourceId}_frontSymbols`, `${sourceId}_frontSymbolOutline`].forEach((id) => { if (map.getLayer(id)) map.removeLayer(id); }); [`${sourceId}_frontSymbolSource`, `${sourceId}_stationarySegmentSource`].forEach((id) => { if (map.getSource(id)) map.removeSource(id); }); }
const toLngLat = (map, x, y) => { const p = map.unproject([x, y]); return [p.lng, p.lat]; };
function triangleCoordinates(map, x, y, ux, uy, nx, ny, side) { const s = FRONT_TRIANGLE_SIZE; return [[toLngLat(map, x - ux * s, y - uy * s), toLngLat(map, x + ux * s, y + uy * s), toLngLat(map, x + nx * side * s * 1.35, y + ny * side * s * 1.35), toLngLat(map, x - ux * s, y - uy * s)]]; }
function semicircleCoordinates(map, x, y, ux, uy, nx, ny, side) { const r = FRONT_SYMBOL_RADIUS; const points = []; for (let i = 0; i <= 12; i += 1) { const theta = Math.PI - (Math.PI * i) / 12; const px = x + ux * r * Math.cos(theta) + nx * side * r * Math.sin(theta); const py = y + uy * r * Math.cos(theta) + ny * side * r * Math.sin(theta); points.push(toLngLat(map, px, py)); } points.push(toLngLat(map, x - ux * r, y - uy * r)); return [points]; }
function buildFrontSymbolFeatures(map, coordinates, frontType) { const normalized = normalizeFrontType(frontType); const style = FRONT_TYPES[normalized]; const features = []; if (!map || !Array.isArray(coordinates) || coordinates.length < 2) return features; let carry = 0; let symbolIndex = 0; for (let i = 0; i < coordinates.length - 1; i += 1) { const p1 = map.project(coordinates[i]); const p2 = map.project(coordinates[i + 1]); const dx = p2.x - p1.x; const dy = p2.y - p1.y; const len = Math.hypot(dx, dy); if (len < 1) continue; const ux = dx / len; const uy = dy / len; const nx = -uy; const ny = ux; let distance = style.spacing - carry; while (distance < len) { const symbol = style.symbols[symbolIndex % style.symbols.length]; const x = p1.x + ux * distance; const y = p1.y + uy * distance; const coordinatesForShape = symbol.kind === 'triangle' ? triangleCoordinates(map, x, y, ux, uy, nx, ny, symbol.side) : semicircleCoordinates(map, x, y, ux, uy, nx, ny, symbol.side); features.push({ type: 'Feature', geometry: { type: 'Polygon', coordinates: coordinatesForShape }, properties: { color: symbol.color } }); symbolIndex += 1; distance += style.spacing; } carry = len - (distance - style.spacing); } return features; }
function buildStationarySegmentFeatures(map, coordinates) { const features = []; if (!map || !Array.isArray(coordinates) || coordinates.length < 2) return features; let carry = 0; let segmentIndex = 0; const period = STATIONARY_SEGMENT_LENGTH + STATIONARY_SEGMENT_GAP; for (let i = 0; i < coordinates.length - 1; i += 1) { const p1 = map.project(coordinates[i]); const p2 = map.project(coordinates[i + 1]); const dx = p2.x - p1.x; const dy = p2.y - p1.y; const len = Math.hypot(dx, dy); if (len < 1) continue; let distance = Math.max(0, period - carry); while (distance < len) { const endDistance = Math.min(len, distance + STATIONARY_SEGMENT_LENGTH); const t1 = distance / len; const t2 = endDistance / len; const start = map.unproject([p1.x + dx * t1, p1.y + dy * t1]); const end = map.unproject([p1.x + dx * t2, p1.y + dy * t2]); features.push({ type: 'Feature', geometry: { type: 'LineString', coordinates: [[start.lng, start.lat], [end.lng, end.lat]] }, properties: { color: STATIONARY_SEGMENT_COLORS[segmentIndex % STATIONARY_SEGMENT_COLORS.length] } }); segmentIndex += 1; distance += period; } carry = len - (distance - period); } return features; }
function renderFrontLayers(map, sourceId, geojson, frontType) { const frozenFrontType = normalizeFrontType(frontType); const style = FRONT_TYPES[frozenFrontType]; const coordinates = geojson?.features?.[0]?.geometry?.coordinates || []; removeFrontLayers(map, sourceId); if (map.getSource(sourceId)) map.getSource(sourceId).setData(geojson); else map.addSource(sourceId, { type: 'geojson', data: geojson }); if (frozenFrontType === 'stationary') { map.addSource(`${sourceId}_stationarySegmentSource`, { type: 'geojson', data: { type: 'FeatureCollection', features: buildStationarySegmentFeatures(map, coordinates) } }); addFrontLineLayer(map, `${sourceId}_dash`, `${sourceId}_stationarySegmentSource`, { 'line-color': ['get', 'color'], 'line-width': style.lineWidth, 'line-opacity': 1 }); } else { addFrontLineLayer(map, `${sourceId}_dash`, sourceId, { 'line-color': style.color, 'line-width': style.lineWidth, 'line-opacity': 1 }); } map.addSource(`${sourceId}_frontSymbolSource`, { type: 'geojson', data: { type: 'FeatureCollection', features: buildFrontSymbolFeatures(map, coordinates, frozenFrontType) } }); map.addLayer({ id: `${sourceId}_frontSymbols`, type: 'fill', source: `${sourceId}_frontSymbolSource`, paint: { 'fill-color': ['get', 'color'], 'fill-opacity': 1 } }); }
function makePreviewSymbols(points, frontType) { const normalized = normalizeFrontType(frontType); const style = FRONT_TYPES[normalized]; if (!Array.isArray(points) || points.length < 4) return []; const symbols = []; let carry = 0; let symbolIndex = 0; for (let i = 0; i < points.length - 2; i += 2) { const x1 = points[i], y1 = points[i + 1], x2 = points[i + 2], y2 = points[i + 3]; const dx = x2 - x1, dy = y2 - y1, len = Math.hypot(dx, dy); if (len < 1) continue; const ux = dx / len, uy = dy / len, nx = -uy, ny = ux; let distance = style.spacing - carry; while (distance < len) { const symbol = style.symbols[symbolIndex % style.symbols.length]; symbols.push({ id: `${i}-${distance}-${symbolIndex}`, kind: symbol.kind, color: symbol.color, x: x1 + ux * distance, y: y1 + uy * distance, rotation: Math.atan2(dy, dx) * 180 / Math.PI + (symbol.kind === 'triangle' ? 90 : 0), side: symbol.side }); symbolIndex += 1; distance += style.spacing; } carry = len - (distance - style.spacing); } return symbols; }
function makeStationaryPreviewSegments(points) { if (!Array.isArray(points) || points.length < 4) return []; const segments = []; let carry = 0; let segmentIndex = 0; const period = STATIONARY_SEGMENT_LENGTH + STATIONARY_SEGMENT_GAP; for (let i = 0; i < points.length - 2; i += 2) { const x1 = points[i], y1 = points[i + 1], x2 = points[i + 2], y2 = points[i + 3]; const dx = x2 - x1, dy = y2 - y1, len = Math.hypot(dx, dy); if (len < 1) continue; let distance = Math.max(0, period - carry); while (distance < len) { const end = Math.min(len, distance + STATIONARY_SEGMENT_LENGTH); const t1 = distance / len; const t2 = end / len; segments.push({ id: `${i}-${distance}-${segmentIndex}`, color: STATIONARY_SEGMENT_COLORS[segmentIndex % STATIONARY_SEGMENT_COLORS.length], points: [x1 + dx * t1, y1 + dy * t1, x1 + dx * t2, y1 + dy * t2] }); segmentIndex += 1; distance += period; } carry = len - (distance - period); } return segments; }

const SurfaceFrontPanel = ({ frontType, setFrontType, isDarkMode }) => { const dragRef = useRef(null); const [position, setPosition] = useState(() => getSafePosition(getDefaultPosition())); const [isDragging, setIsDragging] = useState(false); const positionRef = useRef(position); useEffect(() => { try { const saved = window.localStorage.getItem(STORAGE_KEY); const parsed = saved ? JSON.parse(saved) : null; const safe = getSafePosition(Number.isFinite(parsed?.x) && Number.isFinite(parsed?.y) ? parsed : getDefaultPosition()); positionRef.current = safe; setPosition(safe); } catch { const safe = getSafePosition(getDefaultPosition()); positionRef.current = safe; setPosition(safe); } }, []); useEffect(() => { const handleResize = () => { const safe = getSafePosition(positionRef.current); positionRef.current = safe; setPosition(safe); }; window.addEventListener('resize', handleResize); return () => window.removeEventListener('resize', handleResize); }, []); const persistPosition = useCallback((next) => { try { window.localStorage.setItem(STORAGE_KEY, JSON.stringify(next)); } catch { } }, []); const handleDragStart = useCallback((event) => { if (event.button !== undefined && event.button !== 0) return; event.preventDefault(); event.stopPropagation(); dragRef.current = { pointerId: event.pointerId, startX: event.clientX, startY: event.clientY, originX: positionRef.current.x, originY: positionRef.current.y }; setIsDragging(true); event.currentTarget.setPointerCapture?.(event.pointerId); }, []); const handleDragMove = useCallback((event) => { const drag = dragRef.current; if (!drag || drag.pointerId !== event.pointerId) return; event.preventDefault(); const next = getSafePosition({ x: drag.originX + event.clientX - drag.startX, y: drag.originY + event.clientY - drag.startY }); positionRef.current = next; setPosition(next); }, []); const handleDragEnd = useCallback((event) => { if (!dragRef.current || dragRef.current.pointerId !== event.pointerId) return; event.currentTarget.releasePointerCapture?.(event.pointerId); dragRef.current = null; setIsDragging(false); persistPosition(positionRef.current); }, [persistPosition]); const resetPosition = useCallback((event) => { event.stopPropagation(); const safe = getSafePosition(getDefaultPosition()); positionRef.current = safe; setPosition(safe); persistPosition(safe); }, [persistPosition]); return <div className={cn('studio-liquid-panel fixed z-[110] rounded-2xl border p-3 shadow-2xl', isDragging && 'select-none shadow-[0_26px_70px_rgba(34,211,238,0.22)]', isDarkMode ? 'studio-liquid-dark border-white/[0.18] text-white' : 'studio-liquid-light border-white/80 text-slate-950')} style={{ left: position.x, top: position.y, width: `min(${PANEL_WIDTH}px, calc(100vw - 2rem))` }}><div className="mb-3 flex items-center justify-between gap-2"><button type="button" className="flex min-w-0 flex-1 cursor-grab touch-none items-center gap-2 text-left active:cursor-grabbing" onPointerDown={handleDragStart} onPointerMove={handleDragMove} onPointerUp={handleDragEnd} onPointerCancel={handleDragEnd} title="Drag surface fronts panel"><span className={cn('flex h-9 w-9 shrink-0 items-center justify-center rounded-xl', isDarkMode ? 'bg-cyan-400/12 text-cyan-300' : 'bg-blue-500/10 text-blue-600')}><GripHorizontal size={16} /></span><div><div className="text-[11px] font-black uppercase tracking-wide">Surface Fronts</div><div className={cn('text-[10px] font-bold', isDarkMode ? 'text-white/45' : 'text-slate-500')}>{FRONT_TYPES[frontType]?.fullLabel}</div></div></button><button type="button" onClick={resetPosition} className={cn('flex h-9 w-9 shrink-0 items-center justify-center rounded-xl border', isDarkMode ? 'border-white/10 bg-white/[0.055] text-white/70' : 'border-white/80 bg-white/60 text-slate-600')} title="Reset panel position"><RotateCcw size={14} /></button></div><div className="grid grid-cols-2 gap-2">{Object.entries(FRONT_TYPES).map(([key, config]) => { const Icon = config.icon; const active = frontType === key; return <button key={key} type="button" onClick={() => setFrontType(key)} className={cn('flex h-10 items-center justify-center gap-2 rounded-xl border text-[10px] font-black uppercase tracking-wide transition-all', active ? isDarkMode ? 'border-cyan-300/35 bg-cyan-400/18 text-cyan-100' : 'border-blue-300 bg-blue-50 text-blue-700' : isDarkMode ? 'border-white/8 bg-white/[0.04] text-white/50 hover:bg-white/[0.08] hover:text-white' : 'border-white/70 bg-white/45 text-slate-500 hover:bg-white hover:text-slate-800')}><Icon size={14} /><span>{config.label}</span></button>; })}</div></div>; };
const FlagCanvas = ({ mapRef, isDarkMode, setLayersRef }) => { const [projectId] = useProjectId(); const [lines, setLines] = useState([]); const [frontType, setFrontType] = useState('cold'); const [stageBounds, setStageBounds] = useState(() => getStageBounds(mapRef)); const activeLineRef = useRef(null); const isFlagDrawing = useRef(false); const getAdjustedPoint = useCallback((event) => { const pos = getPointerPosition(event); return { x: pos?.x || 0, y: pos?.y || 0 }; }, []); const refreshStageBounds = useCallback(() => { const next = getStageBounds(mapRef); setStageBounds(next); return next; }, [mapRef]); useEffect(() => { const preventScroll = (event) => { if (isFlagDrawing.current) event.preventDefault(); }; const handleResize = () => refreshStageBounds(); refreshStageBounds(); document.body.addEventListener('touchmove', preventScroll, { passive: false }); window.addEventListener('resize', handleResize); return () => { document.body.removeEventListener('touchmove', preventScroll); window.removeEventListener('resize', handleResize); }; }, [refreshStageBounds]); const clearActiveLine = useCallback(() => { activeLineRef.current = null; setLines([]); }, []); const handleDown = useCallback((event) => { const frozenFrontType = normalizeFrontType(frontType); refreshStageBounds(); isFlagDrawing.current = true; const pos = getAdjustedPoint(event); const nextLine = { points: [pos.x, pos.y], frontType: frozenFrontType }; activeLineRef.current = nextLine; setLines([nextLine]); }, [frontType, getAdjustedPoint, refreshStageBounds]); const handleMove = useCallback((event) => { if (!isFlagDrawing.current) return; const pos = getAdjustedPoint(event); const currentLine = activeLineRef.current; if (!currentLine) return; if (getDistanceFromLastPoint(currentLine.points, pos.x, pos.y) < MIN_POINT_DISTANCE) return; const nextLine = { ...currentLine, points: [...currentLine.points, pos.x, pos.y] }; activeLineRef.current = nextLine; setLines([nextLine]); }, [getAdjustedPoint]); const handleUp = useCallback(async () => { const map = mapRef.current; const line = activeLineRef.current; const frozenFrontType = normalizeFrontType(line?.frontType || frontType); isFlagDrawing.current = false; if (!map || !line?.points || line.points.length < 4) { clearActiveLine(); return; } if (!projectId) { clearActiveLine(); await Swal.fire({ icon: 'warning', title: 'No Project Selected', text: 'Please create or select a valid project before saving surface fronts.', confirmButtonColor: isDarkMode ? '#6366f1' : '#3b82f6' }); return; } const coords = []; for (let i = 0; i < line.points.length; i += 2) { const lngLat = map.unproject([line.points[i], line.points[i + 1]]); coords.push([lngLat.lng, lngLat.lat]); } const owner = JSON.parse(localStorage.getItem('user') || 'null'); const sourceId = `SF_${uuidv4()}`; const name = buildFrontName(frozenFrontType); const feature = { type: 'Feature', geometry: { type: 'LineString', coordinates: coords }, properties: { isFront: true, frontType: frozenFrontType, owner: owner?.id, project: projectId, sourceId } }; const geojson = { type: 'FeatureCollection', features: [feature] }; try { await createFeature({ geometry: feature.geometry, properties: feature.properties, name, sourceId }); if (typeof setLayersRef?.current === 'function') setLayersRef.current((prevLayers) => [{ id: sourceId, sourceID: sourceId, name, visible: true, locked: false, type: name }, ...prevLayers.filter((layer) => layer.id !== sourceId && layer.sourceID !== sourceId)]); try { renderFrontLayers(map, sourceId, geojson, frozenFrontType); } catch (renderError) { console.warn('Surface front saved but could not be rendered locally:', renderError); } } catch (err) { console.error('Error saving surface front:', err); await Swal.fire({ icon: 'error', title: 'Save Failed', text: err?.message || 'Could not save surface front.', confirmButtonColor: isDarkMode ? '#6366f1' : '#3b82f6' }); removeFrontLayers(map, sourceId); if (map.getSource(sourceId)) map.removeSource(sourceId); } finally { clearActiveLine(); } }, [clearActiveLine, frontType, isDarkMode, mapRef, projectId, setLayersRef]); const previewLine = lines[lines.length - 1]; const previewFrontType = normalizeFrontType(previewLine?.frontType || frontType); const previewStyle = FRONT_TYPES[previewFrontType]; const previewSymbols = previewLine ? makePreviewSymbols(previewLine.points, previewFrontType) : []; const stationarySegments = previewFrontType === 'stationary' && previewLine ? makeStationaryPreviewSegments(previewLine.points) : []; return <><SurfaceFrontPanel frontType={frontType} setFrontType={setFrontType} isDarkMode={isDarkMode} /><Stage width={stageBounds.width} height={stageBounds.height} onPointerDown={handleDown} onPointerMove={handleMove} onPointerUp={handleUp} style={{ position: 'fixed', top: stageBounds.top, left: stageBounds.left, width: stageBounds.width, height: stageBounds.height, zIndex: 10, pointerEvents: 'auto' }}><Layer>{previewFrontType === 'stationary' ? stationarySegments.map((segment) => <Line key={segment.id} points={segment.points} stroke={segment.color} strokeWidth={previewStyle.lineWidth} tension={0.45} lineCap="round" lineJoin="round" />) : lines.map((line, i) => { const style = FRONT_TYPES[normalizeFrontType(line.frontType)]; return <Line key={i} points={line.points} stroke={style.color} strokeWidth={style.lineWidth} dash={style.dash || []} tension={0.45} lineCap="round" lineJoin="round" />; })}{previewSymbols.map((symbol) => symbol.kind === 'triangle' ? <RegularPolygon key={symbol.id} x={symbol.x} y={symbol.y} sides={3} radius={10} fill={symbol.color} rotation={symbol.rotation} /> : <Wedge key={symbol.id} x={symbol.x} y={symbol.y} radius={9} angle={180} fill={symbol.color} rotation={symbol.rotation + (symbol.side < 0 ? 180 : 0)} />)}</Layer></Stage></>; };
export default FlagCanvas;
